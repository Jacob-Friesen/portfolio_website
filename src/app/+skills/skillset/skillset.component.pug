.skillset
  .card([class.expanded]='index === collapseManager.openIndex', *ngFor='let skill of data; let index=index')
    .card-header((click)='collapseManager.toggle(index)')
      button.expander
        i.fa.fa-chevron-up(*ngIf='index === collapseManager.openIndex')
        i.fa.fa-chevron-down(*ngIf='index !== collapseManager.openIndex')
      h3.title
        span {{ skill.name }}
        span.level &nbsp;{{ skill.level }} ({{ skill.time.join('') }} years)
    .card-body(*ngIf='index === collapseManager.openIndex')
      hr.separator
      p {{ skill.summary || 'A fake summary here.' }}

      ul.points
        li(*ngFor='let subSkill of skill.subSkills')
          span {{ subSkill.name }}
          span &nbsp;{{ subSkill.level }} ({{ subSkill.time.join('') }} years)
        