var assert = chai.assert;

describe('HelloController', function(){
	//initialise module
    //beforeEach(angular.module('myapp.HelloModule'));

   	it('should run a basic test', function(){
   		assert.equal(1, 1);
	});
});